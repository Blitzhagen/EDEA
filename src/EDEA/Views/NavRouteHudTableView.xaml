<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="EDEA.Views.NavRouteHudTableView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:converter="clr-namespace:EDEA.Converters" xmlns:local="clr-namespace:EDEA.Views" xmlns:mns="clr-namespace:EDEA" xmlns:i="http://schemas.microsoft.com/xaml/behaviors" xmlns:viewmodels="clr-namespace:EDEA.ViewModels">
  <Grid Background="#00FFFFFF">
    <TextBlock Style="{StaticResource TableViewInfoHudBanner}" Text="{Binding NoRouteInfo}" Visibility="{Binding Path=ShowNoRouteInfo, Converter={StaticResource BoolToVis}}" />
    <DataGrid Name="RouteDataGrid" AutoGenerateColumns="False" IsReadOnly="True" Style="{StaticResource DataGridHudStyle}" HeadersVisibility="None" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" ItemsSource="{Binding Route}">
      <DataGrid.RowStyle>
        <Style BasedOn="{StaticResource DataGridHudRowStyle}" TargetType="{x:Type DataGridRow}" />
      </DataGrid.RowStyle>
      <DataGrid.CellStyle>
        <Style BasedOn="{StaticResource DataGridCellStyle}" TargetType="{x:Type DataGridCell}">
          <Style.Triggers>
            <Trigger Property="DataGridCell.IsSelected" Value="True">
              <Setter Property="Background" Value="#00FFFFFF" />
            </Trigger>
          </Style.Triggers>
        </Style>
      </DataGrid.CellStyle>
      <FrameworkElement.Resources>
        <ResourceDictionary>
          <Style x:Key="{x:Type ScrollBar}" TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource MainScrollBarStyle}" />
        </ResourceDictionary>
      </FrameworkElement.Resources>
      <DataGrid.Columns>
        <DataGridTemplateColumn>
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
              <Grid Style="{StaticResource IconAndTextGrid}">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="26" />
                  <ColumnDefinition Width="7" />
                  <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <ContentControl Grid.Column="0" ContentTemplate="{StaticResource IconStarSystemCurrentAndJumpDestinationTemplate}" Content="{Binding}" />
                <ContentControl Grid.Column="1" ContentTemplate="{StaticResource IconStarSystemEdsmOnlyTemplate}" Content="{Binding}" />
                <ContentControl Grid.Column="2" ContentTemplate="{StaticResource TextStarSystemJumpDistanceTemplate}" Content="{Binding}" />
              </Grid>
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
        <DataGridTemplateColumn CellTemplate="{StaticResource TextStarSystemNameTemplate}" Visibility="{Binding Source={x:Static mns:Preferences.HudWindow}, Path=RouteColumnSystemNameVisible, Mode=OneWay, Converter={StaticResource BoolToVis}}" />
        <DataGridTemplateColumn Visibility="{Binding Source={x:Static mns:Preferences.HudWindow}, Path=RouteColumnStarClassVisible, Mode=OneWay, Converter={StaticResource BoolToVis}}">
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
              <Grid Style="{StaticResource IconAndTextGrid}">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition />
                  <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <ContentControl Grid.Column="0" ContentTemplate="{StaticResource IconStarSystemScoopableTemplate}" Content="{Binding}" />
                <ContentControl Grid.Column="1" ContentTemplate="{StaticResource TextStarSystemStarClassTemplate}" Content="{Binding}" />
              </Grid>
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
        <DataGridTemplateColumn Visibility="{Binding Source={x:Static mns:Preferences.HudWindow}, Path=RouteColumnExplorationStatusIconVisible, Mode=OneWay, Converter={StaticResource BoolToVis}}">
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
              <Grid Style="{StaticResource IconAndTextGrid}">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <FrameworkElement.ToolTip>
                  <ContentControl ContentTemplate="{StaticResource ToolTipDiscoveryStatusTemplate}" Content="{Binding}" />
                </FrameworkElement.ToolTip>
                <ContentControl Grid.Column="0" ContentTemplate="{StaticResource IconStarSystemDiscoveryStatusTemplate}" Content="{Binding}" />
              </Grid>
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
        <DataGridTextColumn ElementStyle="{StaticResource TextColumnSystemCartographicValueStyle}" Binding="{Binding BodiesCartographicMaxValue}" Visibility="{Binding Source={x:Static mns:Preferences.HudWindow}, Path=RouteColumnCartographicValueVisible, Mode=OneWay, Converter={StaticResource BoolToVis}}" />
        <DataGridTextColumn ElementStyle="{StaticResource TextColumnSystemProgressStyle}" Binding="{Binding OverallProgress}" Visibility="{Binding Source={x:Static mns:Preferences.HudWindow}, Path=RouteColumnProgressVisible, Mode=OneWay, Converter={StaticResource BoolToVis}}" />
        <DataGridTemplateColumn CellTemplate="{StaticResource IconStarSystemPlanetOfInterestTemplate}" Visibility="{Binding Source={x:Static mns:Preferences.HudWindow}, Path=RouteColumnPlanetOfInterestIconVisible, Mode=OneWay, Converter={StaticResource BoolToVis}}" />
        <DataGridTemplateColumn CellTemplate="{StaticResource IconStarSystemValuableTemplate}" Visibility="{Binding Source={x:Static mns:Preferences.HudWindow}, Path=RouteColumnValuableIconVisible, Mode=OneWay, Converter={StaticResource BoolToVis}}" />
        <DataGridTemplateColumn CellTemplate="{StaticResource IconStarSystemTerraformableTemplate}" Visibility="{Binding Source={x:Static mns:Preferences.HudWindow}, Path=RouteColumnTerraformableIconVisible, Mode=OneWay, Converter={StaticResource BoolToVis}}" />
        <DataGridTemplateColumn CellTemplate="{StaticResource IconStarSystemLandableTemplate}" Visibility="{Binding Source={x:Static mns:Preferences.HudWindow}, Path=RouteColumnLandableIconVisible, Mode=OneWay, Converter={StaticResource BoolToVis}}" />
        <DataGridTemplateColumn CellTemplate="{StaticResource IconStarSystemPopulatedTemplate}" Visibility="{Binding Source={x:Static mns:Preferences.HudWindow}, Path=RouteColumnPopulatedIconVisible, Mode=OneWay, Converter={StaticResource BoolToVis}}" />
        <DataGridTextColumn ElementStyle="{StaticResource TextColumnCommanderNameStyle}" Binding="{Binding EdsmDiscoveryCommander}" Visibility="{Binding Source={x:Static mns:Preferences.HudWindow}, Path=RouteColumnEdsmDiscovererVisible, Mode=OneWay, Converter={StaticResource BoolToVis}}" />
        <DataGridTemplateColumn CellTemplate="{StaticResource TextStarSystemJumpDistanceLyTemplate}" Visibility="{Binding Source={x:Static mns:Preferences.HudWindow}, Path=RouteColumnDistanceVisible, Mode=OneWay, Converter={StaticResource BoolToVis}}" />
      </DataGrid.Columns>
    </DataGrid>
  </Grid>
</UserControl>
